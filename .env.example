# Database — MySQL on the same Hostinger account as the app.
# Production: hPanel → Website → Environment variables (never committed).
# Local: copy to .env, which .gitignore already excludes.
DB_HOST=localhost
DB_PORT=3306
DB_USER=u666480115_xstudiozfolio
DB_PASSWORD=
DB_NAME=u666480115_XStudiozfolio
